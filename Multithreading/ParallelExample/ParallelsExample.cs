// using System.Collections.Concurrent;

namespace CSharpAppPlayground.Multithreading.ParallelExample
{
    public class ParallelsExample
    {
        public void Show()
        {
            // ConcurrentDictionary<int, int> threadLog = new ConcurrentDictionary<int, int>();
            // List<int> itemsToProcess = Enumerable.Range(1, 20).ToList();
        }
    }
}
